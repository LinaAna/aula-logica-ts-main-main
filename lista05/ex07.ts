function area(base, hauteur) {
        return base * hauteur;
        }

        let base = Number(prompt("Base:"));
        let hauteur = Number(prompt("Altura:"));

        alert("Área = " + area(base, hauteur));
