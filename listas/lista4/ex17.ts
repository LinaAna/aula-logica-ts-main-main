let votos = ["sim", "sim", "nao", "sim", "nao"];
let qtdSim = 0;

for (let i = 0; i < votos.length; i++) {
    if (votos[i] === "sim") {
            qtdSim++;
                }
                }

                alert(qtdSim + " votos 'sim'");